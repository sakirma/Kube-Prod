nodeSelector:
  ingress-ready: "true"
  kubernetes.io/os: linux
tolerations:
  - effect: NoSchedule
    key: node-role.kubernetes.io/control-plane
    operator: Equal
service:
  ports:
    - name: http2
      port: 8080
      protocol: TCP
      targetPort: 80
